class RECTANGLE {
  private:
    double LENGTH__;
    double WIDTH__;

  private:
    RECTANGLE();
    RECTANGLE(double l, double w);
    void SET_LENGTH(double l);
    void SET_WIDTH(double w);
    double GET_LENGTH();
    double GET_WIDTH();
    ~RECTANGLE;
}
